---
import Head from "../components/Head.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Script from "../components/Script.astro";
import "../styles/global.scss";

export interface Props {
  page: string;
  title: string;
  description: string;
  keywords: string;
  author: string;
}

const { page, title, description, keywords, author } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <Head title={title} description={description} keywords={keywords} author={author} />
  <body class="bg-gray-900 text-white scrollbar scrollbar-thumb-gray-600">
    <div class="flex flex-col h-screen justify-between">
      <Header page={page} />
      <main>
        <slot />
      </main>
      <Footer />
    </div>
    <Script /> 
  </body>
</html>
